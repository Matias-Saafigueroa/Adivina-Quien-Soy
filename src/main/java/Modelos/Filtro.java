package Modelos;

/**
 * Las 6 características por las que se puede preguntar para acortar la búsqueda.
 */
public enum Filtro {
    GENERO,
    CALVICIE,
    LENTES,
    COLOR_PELO,
    EDAD,
    GRUPO
}
